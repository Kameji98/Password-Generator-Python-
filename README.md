# Password Generator (Python)

A small CLI tool that generates secure passwords using cryptographically strong randomness.

## Features
- Custom password length (8–64)
- Optional character groups:
  - lowercase letters
  - uppercase letters
  - digits
  - symbols
- Input validation and friendly CLI UX
- Simple strength hint based on password length and character set size

## Tech Stack
- Python 3 (standard library only)

## Run Locally
```bash
python src/main.py
